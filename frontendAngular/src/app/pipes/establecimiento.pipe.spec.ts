import { EstablecimientoPipe } from './establecimiento.pipe';

describe('EstablecimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstablecimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
